export GOWINHOME=/Applications/GowinIDE.app/Contents/Resources/Gowin_EDA
export DYLD_LIBRARY_PATH=/Applications/GowinIDE.app/Contents/Resources/Gowin_EDA/IDE/lib
export DYLD_FRAMEWORK_PATH=/Applications/GowinIDE.app/Contents/Resources/Gowin_EDA/IDE/lib
# D101: the arch-gen determinism check imports `apycula` and its built
# <device>.msgpack.xz from the sibling apicula checkout -- the one this
# nextpnr checkout is paired with. Per-task worktrees come and go; the
# sibling does not. Override in the environment to point elsewhere.
export APICULA_ROOT=/Users/alex/fine-line/.atelier/worktrees/2026-09-03-open-toolchain-gw5ast-7e84/apicula
# apicula's deps (msgpack, numpy) live in the project venv, not in system python3.
export GATE_PYTHON=/Users/alex/fine-line/vendor/venv/bin/python
